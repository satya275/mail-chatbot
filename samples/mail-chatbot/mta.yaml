_schema-version: "3.1"
ID: mail-chatbot-usecase
description: A simple CAP project.
version: 1.0.0

parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm install
        - npx cds build --production

modules:
  # --------------------- SERVER MODULE -------------------------
  - name: mail-chatbot-usecase-srv
    # -----------------------------------------------------------
    type: nodejs
    path: gen/srv
    requires:
      - name: mail-chatbot-usecase-auth
      - name: mail-chatbot-usecase-db
      - name: mail-chatbot-usecase-destination-service
      - name: mail-chatbot-usecase-connectivity
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
      keep-existing-routes: true
      disk-quota: 1024M
      memory: 512M
    build-parameters:
      builder: npm
      ignore:
        - .env
        - node_modules/
        - default-env.json

  # -------------------- DB MODULE ------------------------------
  - name: mail-chatbot-usecase-db-deployer
    # -----------------------------------------------------------
    type: hdb
    path: gen/db
    requires:
      - name: mail-chatbot-usecase-db
    parameters:
      buildpack: nodejs_buildpack
    build-parameters:
      ignore:
        - node_modules/

  # ------------------ DESTINATION CONTENT ----------------------
  - name: mail-chatbot-usecase-destination-content
    # -----------------------------------------------------------
    type: com.sap.application.content
    requires:
      - name: mail-chatbot-usecase-destination-service
        parameters:
          content-target: true
      - name: mail-chatbot-usecase-repo-host
        parameters:
          service-key:
            name: mail-chatbot-usecase-repo-host-key
      - name: mail-chatbot-usecase-auth
        parameters:
          service-key:
            name: uaa_mail-chatbot-usecase-key
    parameters:
      content:
        instance:
          destinations:
            - Name: mail_chatbot_usecase_repo_host
              ServiceInstanceName: mail-chatbot-usecase-html5-app-host-service
              ServiceKeyName: mail-chatbot-usecase-repo-host-key
              sap.cloud.service: mail.app
            - Authentication: OAuth2UserTokenExchange
              Name: mail_uaa_mail_chatbot_usecase
              ServiceInstanceName: mail-chatbot-usecase-xsuaa-service
              ServiceKeyName: uaa_mail-chatbot-usecase-key
              sap.cloud.service: mail.app
          existing_destinations_policy: ignore
    build-parameters:
      no-source: true

  # ------------------ APPLICATION CONTENT ----------------------
  # ------------------ App Router -------------------------------
  - name: mail-chatbot-usecase-approuter
    # -----------------------------------------------------------
    type: approuter.nodejs
    path: app/
    parameters:
      keep-existing-routes: true
      disk-quota: 1024M
      memory: 256M
    build-parameters:
      ignore:
        - .env
        - node_modules/
        - default-env.json
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api          # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
      - name: mail-chatbot-usecase-auth

resources:
  # -------------------- XSUAA ----------------------------------
  - name: mail-chatbot-usecase-auth
    # -----------------------------------------------------------
    type: org.cloudfoundry.managed-service
    parameters:
      path: ./xs-security.json
      service: xsuaa
      service-plan: application
      service-name: mail-chatbot-usecase-xsuaa-service
      config:
        tenant-mode: dedicated
        xsappname: mail-chatbot-usecase

  # -------------------- HANA HDI -------------------------------
  - name: mail-chatbot-usecase-db
    # -----------------------------------------------------------
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared

  # ---------------- HTML5 Repo Host ----------------------------
  - name: mail-chatbot-usecase-repo-host
    # -----------------------------------------------------------
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: mail-chatbot-usecase-html5-app-host-service
      service-plan: app-host

  # ---------------- Destination Service ------------------------
  - name: mail-chatbot-usecase-destination-service
    # -----------------------------------------------------------
    type: org.cloudfoundry.managed-service
    requires:
      - name: srv-api
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          subaccount:
            destinations:
              - Authentication: NoAuthentication
                HTML5.DynamicDestination: true
                HTML5.ForwardAuthToken: true
                HTML5.Timeout: 30000
                Name: mail-chatbot-usecase-srv
                ProxyType: Internet
                Type: HTTP
                URL: ~{srv-api/srv-url}
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: mail-chatbot-usecase-destination-service
      service-plan: lite

  # ---------------- Connectivity Service -----------------------
  - name: mail-chatbot-usecase-connectivity
    # -----------------------------------------------------------
    type: org.cloudfoundry.managed-service
    parameters:
      service: connectivity
      service-plan: lite
      service-name: mail-chatbot-usecase-connectivity
